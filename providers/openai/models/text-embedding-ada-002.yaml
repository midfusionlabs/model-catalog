name: "text-embedding-ada-002"
display_name: "Text Embedding Ada 002"
family: "embedding"
status: "stable"

cost:
  input_per_1k: 0.0001
  output_per_1k: 0

limits:
  max_tokens: 8191

capabilities:
  - embeddings

modalities:
  input: ["text"]
  output: ["embedding"]

