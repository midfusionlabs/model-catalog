name: "text-embedding-3-small"
display_name: "Text Embedding 3 Small"
family: "embedding"
status: "stable"

cost:
  input_per_1k: 0.00002
  output_per_1k: 0

limits:
  max_tokens: 8191

capabilities:
  - embeddings

modalities:
  input: ["text"]
  output: ["embedding"]

